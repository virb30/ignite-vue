<template>
  <header>
    <div>
      <img src="../assets/logo.svg" alt="dt money">
      <button type="button" @click="handleOpenNewTransactionModal">
        Nova transação
      </button>
    </div>
  </header>
</template>

<script lang="ts">
import Vue, {} from 'vue';

interface Methods {
  handleOpenNewTransactionModal: () => void;
}

type Data = unknown;

type Computed = unknown;

export default Vue.extend<Data, Methods, Computed, []>({
  methods: {
    handleOpenNewTransactionModal() {
      this.$modal.show('newTransactionsModal');
    },
  },
});
</script>

<style lang="scss" scoped>
header {
  background: var(--blue);

  div {
    max-width: 1120px;
    margin: 0 auto;

    padding: 2rem 1rem 12rem;
    display: flex;
    align-items: center;
    justify-content: space-between;

    button {
      font-size: 1rem;
      color: #FFF;
      background: var(--blue-light);
      border: 0;
      padding: 0 2rem;
      border-radius: 0.25rem;
      height: 3rem;

      transition: filter 0.2s;

      &:hover {
        filter: brightness(0.9);
      }
    }
  }
}
</style>
